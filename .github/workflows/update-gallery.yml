name: Update Gallery

on:
  push:
    branches: [main]
    paths:
      - 'illustrations/**'
      - 'surfaceandpattern/**'
      - 'sketches/**'
      - 'gallery-order.json'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Regenerate gallery-data.js and thumbnails
        run: node build.js

      - name: Commit updated files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add gallery-data.js thumbs/
          git diff --staged --quiet || git commit -m "chore: auto-update gallery and thumbnails"
          git push
